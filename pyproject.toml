[project]
name = "cupboard-cad-benchmark"
version = "0.1.0"
description = "Free CAD-as-code benchmark for generating a cupboard model."
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "build123d>=0.9.1",
    "cadquery>=2.5.2",
    "numpy>=1.26",
    "pillow>=10.0",
    "trimesh>=4.0",
]

[tool.uv]
package = false

