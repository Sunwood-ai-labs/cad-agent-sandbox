[project]
name = "cad-agent-sandbox"
version = "0.1.0"
description = "Windows-first CAD-as-code benchmark workspace for repeatable furniture cases."
readme = "README.md"
requires-python = ">=3.11,<3.12"
license = { text = "MIT" }
dependencies = [
    "build123d>=0.9.1",
    "cadquery>=2.5.2",
    "numpy>=1.26",
    "pillow>=10.0",
    "trimesh>=4.0",
]

[tool.uv]
package = false
